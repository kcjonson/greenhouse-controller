

# upstream web {
#     server web:3001;
# }

upstream api {
    server api:3000 fail_timeout=5s max_fails=5;
}


# server {
#     listen 3000;
#     location / {
#         proxy_set_header  X-Real-IP  $remote_addr;
#         proxy_set_header  X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header Host $http_host;
#         proxy_pass http://app;
#     }
# }

server {
    listen 8090;
    location /api {
        rewrite /api/(.*) /$1  break;
        proxy_set_header  X-Real-IP  $remote_addr;
        proxy_set_header  X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $http_host;
        proxy_pass http://api;
    }
}

